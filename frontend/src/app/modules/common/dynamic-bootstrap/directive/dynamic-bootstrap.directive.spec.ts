import { DynamicBootstrapDirective } from './dynamic-bootstrap.directive';

describe('DynamicBootstrapDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicBootstrapDirective();
    expect(directive).toBeTruthy();
  });
});
