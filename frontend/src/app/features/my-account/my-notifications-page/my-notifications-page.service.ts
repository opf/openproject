import { Injectable } from '@angular/core';
import { NotificationSettingsStore } from "core-app/features/my-account/my-notifications-page/notification-settings.store";

export class MyNotificationsPageService {
  constructor() { }

//   NotificationSettingsStore.update(1, ({ notificationSettings }) => ({
//     notificationSettings: arrayAdd(notificationSettings, newComments)
// }));
}
