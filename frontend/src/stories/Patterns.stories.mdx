import { Meta } from '@storybook/addon-docs';

<Meta title="Patterns" />

# Patterns

Patterns are standards of user interaction that we want to reuse throughout our application. When designing a feature, the team should strive to fall back to these patterns instead of reinventing the wheel.

## Example pattern one

This is an example pattern. For example, we could codify our inline-editing UX, or when to use wizard-esque interactions versus a normal form.
