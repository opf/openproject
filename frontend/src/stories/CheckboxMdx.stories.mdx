import { Meta, Story, Canvas } from '@storybook/addon-docs';
import tokens from '../app/spot/styles/tokens/dist/tokens.json';
import { moduleMetadata } from '@storybook/angular';

import { OpSpotModule } from '../app/spot/spot.module';
import { SpotCheckboxComponent } from '../app/spot/components/checkbox/checkbox.component';
import { CheckboxAngularStoryComponent } from './CheckboxAngular.stories';

<Meta
  title="Blocks/CheckboxMdx"
  component={CheckboxAngularStoryComponent}
/>

# Checkboxes

Checkboxes are used in a wide variety of contexts to allow the user to select or enable multiple options that are often presented in a list.

They may also be used individually in some cases, as a switch (on/off), like in the example above (“Keep me logged in”).

<Canvas>
  <Story name="Default">
    {() => {
      return {
        title: 'Angular',
        component: CheckboxAngularStoryComponent,
      };
    }}
  </Story>
</Canvas>


