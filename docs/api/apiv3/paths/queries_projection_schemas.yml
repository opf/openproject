# /api/v3/queries/projection_schemas
---
get:
  responses:
    '200':
      content:
        application/hal+json:
          examples:
            ProjectQueryProjectionSchemas:
              "$ref": "../components/examples/project_query_projection_schemas.yml"
      description: OK
      headers: {}
    '403':
      content:
        application/hal+json:
          examples:
            response:
              value:
                _type: Error
                errorIdentifier: urn:openproject-org:api:v3:errors:Unauthenticated
                message: You are not authorized to access this resource.
      description: |-
        Returned if the client does not have sufficient permissions to see it.

        **Required permission:** view work package in any project
      headers: {}
  tags:
    - Query Projection Schema
  description: Returns the list of QueryProjection schemas defined for a global
    query, which is a query not assigned to a project.
  operationId: List_Query_Projection_Schemas
  summary: List Query Projection Schemas
