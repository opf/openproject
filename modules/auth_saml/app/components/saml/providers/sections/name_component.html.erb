<%=
  flex_layout do |flex|
    flex.with_column do
      render(Primer::Beta::Text.new(font_weight: :semibold)) { t("saml.providers.singular") }
    end
    flex.with_column do
      render(Primer::Beta::Label.new(scheme: :success, ml: 1)) { t(:label_completed) }
    end
  end
%>
<%=
  render(Primer::Beta::Text.new(tag: :p, font_size: :small, color: :subtle)) do
    t("saml.providers.section_texts.display_name")
  end
%>
