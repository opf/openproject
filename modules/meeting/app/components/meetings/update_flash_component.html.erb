<%= render(Primer::Alpha::Banner.new(
  full: false,
  icon: 'info',
  scheme: :default,
  data: {
    singleton: true
  }
)) do |component| %>
  <% component.with_action_button(
       tag: :a,
       href: meeting_path(@meeting),
       data: { turbo: false },
       size: :medium) { "Reload page" } %>
  You really need to update this upage
<% end %>
