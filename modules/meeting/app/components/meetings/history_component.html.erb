<%=
  component_wrapper do
    # activites fetch goes here

    render(OpTurbo::FrameComponent.new(@meeting, context: :meeting_history, lazy: true, id: @meeting.id)) do
      render(Primer::Beta::Text.new(font_size: :small, color: :subtle)) { t("label_meeting_created_by") }
    end
  end
%>
