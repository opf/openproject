<%=
  component_wrapper do
    primer_form_with(
      model: @allocation,
      scope: :resource_allocation,
      url: form_url,
      method: form_method,
      html: {
        data: {
          turbo_stream: true,
          controller: "refresh-on-form-changes resource-management--created-placeholder-user",
          "refresh-on-form-changes-target": "form",
          "refresh-on-form-changes-turbo-stream-url-value": refresh_form_project_resource_allocations_path(@project),
          "resource-management--created-placeholder-user-dialog-id-value":
            ResourceManagement::PlaceholderUsers::NewDialogComponent::DIALOG_ID,
          "resource-management--created-placeholder-user-autocompleter-value": "opce-resource-allocation-autocompleter"
        },
        id: ResourceAllocations::NewDialogComponent::FORM_ID
      }
    ) do |f|
      render(form_list_component(f))
    end
  end
%>
