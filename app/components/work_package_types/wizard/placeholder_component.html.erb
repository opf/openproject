<%= render(Primer::Beta::Heading.new(tag: :h2, mb: 3)) { step.title } %>

<%= render(Primer::Beta::Blankslate.new) do |component| %>
  <% component.with_visual_icon(icon: :tools) %>
  <% component.with_heading(tag: :h3) { step.title } %>
  <% component.with_description { I18n.t("types.creation_wizard.placeholder_description") } %>
<% end %>
