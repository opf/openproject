<%= render(Primer::Beta::Text.new(tag: :p, color: :muted, mb: 3)) { I18n.t("types.creation_wizard.workflows.intro") } %>

<%= primer_form_with(
      model:,
      url: form_url,
      method: :patch,
      html: { id: WorkPackageTypes::Wizard::FooterComponent::FORM_IDENTIFIER }
    ) do |f| %>
  <%= render(WorkPackageTypes::Wizard::WorkflowsForm.new(f)) %>
<% end %>
