<%=
  render(Primer::Alpha::ActionMenu.new(select_variant: :single, dynamic_label: true, color: :subtle, form_arguments: { builder: @form, name: "role_id" })) do |menu|
    menu.with_show_button do |button|
      button.with_trailing_action_icon(icon: :"triangle-down")
      permission_name(@permission_value)
    end
    options.each do |option|
      menu.with_item(label: option[:label], active: option[:value] == @permission_value, data: { value: option[:value] })
    end
  end
%>
