<ul class="op-project-activity-list">
  <% @events.each do |event| -%>
    <% journal = @journals_by_id[event.event_id] -%>
    <%= render(ActivityItemComponent.new(event:, journal:)) %>
  <% end -%>
</ul>
