<div>
  <div class="meta" id="last_updated"><%= date_string_with_milliseconds( (@last_updated.nil? ? Time.parse(params[:after]) : @last_updated.updated_on) )  %></div>
  <%= render :partial => "task", :collection => @tasks, :locals => { :include_meta => @include_meta } %>
</div>