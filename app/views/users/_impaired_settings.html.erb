<%= fields_for :pref, @user.pref, :lang => current_language do |pref_fields| %>
  <p>
    <%= pref_fields.label(:impaired, UserPreference.human_attribute_name(:impaired)) %>
    <%= pref_fields.check_box :impaired %>
  </p>
<% end %>
