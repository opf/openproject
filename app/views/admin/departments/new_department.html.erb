<%= render(
      Admin::Departments::DetailComponent.new(
        group: @group,
        ancestors: @ancestors || [],
        child_groups: @child_groups,
        add_subgroup: true
      )
    ) %>
