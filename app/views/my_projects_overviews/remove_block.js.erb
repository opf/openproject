(function($) {
  var block = $('#block_' + '<%= @block.is_a?(Array) ? @block.first.dasherize : @block.dasherize %>');
  block.fadeOut('fast', function() {
    block.remove();
    myPage.updateSelect();
  });
}(jQuery))
