<%= header_tags %>

<% html_title l('timelines.reporting_for_project.show',
                :title => h(@reporting.reporting_to_project.name)) %>
<h2>
  <%= l('timelines.reporting_for_project.show',
        :title => link_to_project(@reporting.reporting_to_project)) %>
</h2>

<%= form_for :reporting,
            :url  => project_reporting_path(@project, @reporting),
            :html => {:method => 'delete'} do |f| %>

  <div class='flash warning'>
    <%= l('timelines.really_delete_reporting') %>
  </div>

  <%= render :partial => 'show' %>

  <br/>
  <p>
    <%= f.submit l(:button_delete), :name => nil %>
    <%= link_to l(:button_cancel), project_reportings_path(@project) %>
  </p>
<% end %>
