<%= timelines_header_tags %>
<%= form_for @planning_elements, :url => restore_all_timelines_project_planning_elements_path(@project), :html => {:method => 'post'} do |f| %>
  <div class='flash warning'>
    <%= l('timelines.really_restore_all_planning_elements') %>
  </div>

  <%= submit_tag l("timelines.button_restore_all") %>
  <%= link_to l(:button_cancel), recycle_bin_timelines_project_planning_elements_path(@project) %>
<% end %>

<hr/>

<%= render :partial => 'deleted_elements', :object => @planning_elements %>
