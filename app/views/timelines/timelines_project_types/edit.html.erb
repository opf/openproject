<%= timelines_header_tags %>

<%= form_for(:project_type,
            :url  => timelines_project_type_path(@project_type),
            :html => {:class => "tabular", :method => 'put'}) do |f| %>

  <%= render :partial => "timelines/timelines_project_types/form",
             :locals => {:f => f, :project_type => @project_type} %>
<% end %>
