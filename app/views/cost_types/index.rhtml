<div class="contextual">
<%= link_to l(:button_add_cost_type), {:controller => 'cost_types', :action => 'new' }, :class => 'icon icon-add' %>
</div>

<h2><%= l(:caption_cost_type_plural) %></h2>

<% form_tag({ :controller => 'cost_types', :action => 'index' }, :id => 'query_form') do %>
<fieldset id="filters"><legend><%= l(:label_filter_plural) %></legend>
<p>
  <label><%= l(:label_fixed_date) %></label>
  <%= text_field_tag :fixed_date, @fixed_date, :size => 10 %><%= calendar_for('fixed_date') %>
</p>
<p class="buttons">
<%= link_to_remote l(:button_apply), 
                   { :update => "content",
                     :with => "Form.serialize('query_form')"
                   }, :class => 'icon icon-checked' %>
                   
<%= link_to_remote l(:button_clear),
                   { :url => { :set_filter => 1 }, 
                     :update => "content",
                   }, :class => 'icon icon-reload'  %>
</p>
</fieldset>
<% end %>

<div id="cost_type_flash_notice_outer" style="display:none">
<div class="flash notice" id="cost_type_flash_notice"><%= l(:notice_successful_update) %></div>
</div>
<%= render :partial => 'list' %>

<% content_for :header_tags do %>
  <%= stylesheet_link_tag 'scm' %>
  <%= stylesheet_link_tag 'costs', :plugin => 'redmine_costs' %>
<% end %>
