<%= form_for :planning_element,
		:html => { :id => "planningElementForm" },
		:url => project_planning_elements_path(@project) do |f| %>

  <%= render :partial => 'form', :locals => {:f => f, :planning_element => @planning_element, :project => @project} %>
<% end %>
