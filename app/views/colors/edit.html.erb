<%= header_tags %>

<%= form_for :color,
            :url  => color_url(@color),
            :html => {:method => 'put'} do |f| %>
  <%= render :partial => 'form',
             :locals => {:f => f, :color => @color} %>
<% end %>
