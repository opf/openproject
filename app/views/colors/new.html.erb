<%= header_tags %>

<%= form_for :color,
            :url  => colors_url,
            :html => {:method => 'post'} do |f| %>
  <%= render :partial => 'form',
             :locals => {:f => f, :color => @color} %>
<% end %>
