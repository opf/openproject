<% html_title variant_scope_title,
              adding_variant? ? t("types.creation_wizard.create_variant") : t("types.creation_wizard.create_type") %>

<%= render(
      WorkPackageTypes::Wizard::PageComponent.new(
        type: @type,
        variant: @variant,
        current_step: @current_step,
        back_url: @back_url
      )
    ) %>
