<%=
  I18n.t(:"mail_member_updated_project.body.updated_by.#{@message.present? ? 'with_message' : 'without_message'}",
         project: @project.name,
         user: User.current.name)
%>

<%= I18n.t(:'mail_member_updated_project.body.roles') %>
<% @roles.each do |role| %>
  * <%= h(role.name) %>
<% end %>
