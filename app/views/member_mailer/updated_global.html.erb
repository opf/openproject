<%=
  raw I18n.t(:"mail_member_updated_global.body.updated_by.#{@message.present? ? 'with_message' : 'without_message'}",
             user: link_to_user(User.current, only_path: false))
%>

<%= format_text(@message) %>

<%= I18n.t(:'mail_member_updated_global.body.roles') %>
<ul>
  <% @roles.each do |role| %>
    <li>
      <%= role.name %>
    </li>
  <% end %>
</ul>
