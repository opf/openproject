<h2><%= Document.model_name.human %></h2>

<%= form_tag(document_path(@document),
             :method => :put,
             :class => "tabular") do %>
  <%= render :partial => 'form' %>
  <%= submit_tag l(:button_save) %>
<% end %>


